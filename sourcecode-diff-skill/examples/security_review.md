# Security Review Example

Use `profile=security` for normal security triage and `profile=security-strict` only when LLM review is required.

Do not report inferred findings as confirmed vulnerabilities.

