# Directory Example

```bash
python scripts/run_skill_diff.py --data-folder . --old-root old_src --new-root new_src --language auto --profile security --llm-mode off --exclude-dir vendor
```

Always report skipped files and failed pairs from the generated summary artifacts.

