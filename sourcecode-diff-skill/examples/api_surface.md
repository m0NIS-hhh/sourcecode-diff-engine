# API Surface Example

```bash
python scripts/run_skill_diff.py --data-folder . --old-root old_src --new-root new_src --language auto --profile api-surface --llm-mode off
```

Use this profile to prioritize new routes, handlers, configuration entries, and externally reachable functions.

